# Инструкция по размещению портфолио на GitHub Pages

## 📋 Подготовка файлов

Файлы уже подготовлены:
- ✅ `index.html` - главная страница портфолио
- ✅ `.nojekyll` - файл для отключения обработки Jekyll

## 🚀 Шаги по настройке GitHub Pages

### Вариант 1: Использование существующего репозитория `portfolio`

Если у вас уже есть репозиторий `https://github.com/pyLexxDramma/portfolio`:

1. **Клонируйте репозиторий** (если еще не клонирован):
   ```bash
   git clone https://github.com/pyLexxDramma/portfolio.git
   cd portfolio
   ```

2. **Скопируйте файлы** в репозиторий:
   - Скопируйте `index.html` в корень репозитория
   - Скопируйте `.nojekyll` в корень репозитория

3. **Закоммитьте и отправьте изменения**:
   ```bash
   git add index.html .nojekyll
   git commit -m "Add portfolio for GitHub Pages"
   git push origin main
   ```

4. **Включите GitHub Pages**:
   - Перейдите в репозиторий на GitHub
   - Откройте **Settings** → **Pages**
   - В разделе **Source** выберите:
     - **Branch**: `main` (или `master`)
     - **Folder**: `/ (root)`
   - Нажмите **Save**

5. **Дождитесь публикации**:
   - GitHub Pages обычно публикуется через 1-2 минуты
   - Ваш сайт будет доступен по адресу: `https://pyLexxDramma.github.io/portfolio/`

### Вариант 2: Создание нового репозитория

Если репозитория еще нет:

1. **Создайте новый репозиторий** на GitHub:
   - Название: `portfolio` (или любое другое)
   - Публичный или приватный (GitHub Pages работает с обоими)

2. **Инициализируйте репозиторий локально**:
   ```bash
   mkdir portfolio
   cd portfolio
   git init
   ```

3. **Скопируйте файлы**:
   - Скопируйте `index.html` в папку репозитория
   - Скопируйте `.nojekyll` в папку репозитория

4. **Создайте README.md** (опционально):
   ```markdown
   # Портфолио Алексея Темникова
   
   Fullstack Python Developer
   
   Посетите сайт: https://pyLexxDramma.github.io/portfolio/
   ```

5. **Закоммитьте и отправьте**:
   ```bash
   git add .
   git commit -m "Initial commit: Portfolio website"
   git branch -M main
   git remote add origin https://github.com/pyLexxDramma/portfolio.git
   git push -u origin main
   ```

6. **Включите GitHub Pages** (см. шаг 4 из Варианта 1)

## 🔧 Настройка пользовательского домена (опционально)

Если вы хотите использовать свой домен:

1. **Добавьте файл `CNAME`** в корень репозитория:
   ```
   yourdomain.com
   ```

2. **Настройте DNS записи** у вашего регистратора домена:
   - Тип: `CNAME`
   - Имя: `www` (или `@` для корневого домена)
   - Значение: `pyLexxDramma.github.io`

3. **В настройках GitHub Pages** укажите ваш домен

## 📝 Обновление портфолио

После внесения изменений в `index.html`:

```bash
git add index.html
git commit -m "Update portfolio"
git push origin main
```

Изменения появятся на сайте через 1-2 минуты.

## ✅ Проверка

После настройки проверьте:

1. ✅ Сайт доступен по адресу `https://pyLexxDramma.github.io/portfolio/`
2. ✅ Все стили загружаются корректно
3. ✅ Все ссылки работают
4. ✅ Изображения отображаются (аватар из GitHub)
5. ✅ Навигация работает плавно

## 🐛 Решение проблем

### Сайт не обновляется
- Подождите 2-3 минуты после push
- Очистите кэш браузера (Ctrl+F5)
- Проверьте статус публикации в Settings → Pages

### Стили не применяются
- Убедитесь, что файл `.nojekyll` присутствует в репозитории
- Проверьте, что все CSS находится внутри `<style>` тега в `index.html`

### 404 ошибка
- Убедитесь, что файл называется именно `index.html` (с маленькой буквы)
- Проверьте, что выбрана правильная ветка в настройках Pages
- Убедитесь, что файл находится в корне репозитория

## 📚 Полезные ссылки

- [Документация GitHub Pages](https://docs.github.com/en/pages)
- [Настройка GitHub Pages](https://docs.github.com/en/pages/getting-started-with-github-pages)
- [Пользовательские домены](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site)

---

**Готово!** Ваше портфолио теперь доступно в интернете! 🎉
