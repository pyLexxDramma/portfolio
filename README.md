# Unified Parser - Парсер для Yandex.Maps и 2GIS

Парсер для одновременного сбора данных с Yandex.Maps и 2GIS с веб-интерфейсом и генерацией отчетов.

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Скопируйте `.env.example` в `.env` и заполните своими данными:
```bash
cp .env.example .env
```

3. Отредактируйте `.env` файл:
   - Установите `SITE_PASSWORD` для защиты веб-интерфейса
   - Настройте прокси (если требуется): `PROXY_ENABLED=true`, `PROXY_SERVER`, `PROXY_PORT`, `PROXY_USERNAME`, `PROXY_PASSWORD`
   - Настройте SMTP для email уведомлений (если требуется)

4. Запустите сервер:
```bash
python run_server.py
```

5. Откройте браузер и перейдите на `http://localhost:8000`

## Конфигурация

### Файл `.env`

Создайте файл `.env` в корне проекта на основе `.env.example`. Файл `.env` уже создан с вашими данными.

**Настройка SMTP для email уведомлений:**

#### Для Gmail:
```env
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password  # Используйте App Password, не обычный пароль!
```
⚠️ **Важно для Gmail**: Нужно использовать [App Password](https://support.google.com/accounts/answer/185833), а не обычный пароль. Включите 2FA и создайте App Password в настройках Google Account.

#### Для Yandex:
```env
SMTP_SERVER=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your_email@yandex.ru
SMTP_PASSWORD=your_password
```

#### Для Mail.ru:
```env
SMTP_SERVER=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=your_email@mail.ru
SMTP_PASSWORD=your_password
```

**Примечание**: После настройки SMTP раскомментируйте соответствующие строки в `.env` файле.

### Файл `config/config.json`

Дополнительные настройки парсера (лимиты, таймауты и т.д.) можно настроить в `config/config.json`.

## Использование (пошаговая инструкция для пользователя)

### 1. Запуск сервера

1. Убедитесь, что зависимости установлены (`pip install -r requirements.txt`).
2. Запустите сервер:
   ```bash
   python run_server.py
   ```
3. Откройте браузер (Chrome / Firefox / Opera и т.п.) и перейдите на:
   - `http://localhost:8000` (или порт, который вы указали при запуске).

### 2. Авторизация

1. На странице входа введите пароль:
   - Берётся из переменной окружения `SITE_PASSWORD`, либо из `config/config.json`.
2. Нажмите **«Войти»**.

### 3. Создание задачи на парсинг

На главной странице откроется форма **Web Scraper**.

Заполните поля:

1. **«Введите название компании»**  
   - Пример: `СберБанк`, `ООО Ромашка`.
2. **«Введите сайт компании»**  
   - Пример: `sberbank.ru`, `romashka.ru`.  
   Это помогает точнее отфильтровать чужие компании с похожим названием.
3. **«Выберите источник»**  
   - `Оба источника (Яндекс + 2GIS)` – последовательно пройдут оба парсера.
   - `Яндекс.Карты` – только Яндекс.
   - `2GIS` – только 2ГИС.
4. **«Ваш email»**  
   - Для уведомлений (если настроен SMTP). Можно указать любой рабочий адрес.
5. **«Область поиска»**
   - **`Город`**  
     - Появится поле **«Название города»**.  
       Примеры: `Москва`, `Санкт-Петербург`, `Новосибирск`.  
     - Парсер будет искать карточки только в этом городе.
   - **`Страна / общий поиск`**  
     - Появляется блок **«Регионы и города (от 10 000 жителей)»**:
       - слева список регионов, внутри – города (по данным Росстата, ≥10 000 жителей);
       - сверху строка **«Быстрый поиск по городу…»**:
         - начните вводить название (`новос` → `Новосибирск`, `Новокузнецк` и т.п.);
         - если по фильтру остаётся один город, он автоматически отмечается.
       - выбранные города отображаются под списком в виде «чипов» с крестиком:
         - например: `Москва ×   Санкт-Петербург ×   Новосибирск ×`;
         - по нажатию на **×** город убирается из выбора.
     - При запуске задачи парсер последовательно пройдётся по **каждому выбранному городу**.
6. **«Имя файла отчёта»**
   - Имя итогового CSV‑файла, по умолчанию `report.csv`.  
   - Файл создаётся в папке, заданной в конфиге (обычно `output/`).

После заполнения формы нажмите кнопку **«Поехали!»**.  
Вы будете перенаправлены на страницу статуса задачи.

### 4. Страница статуса задачи

На странице задачи (`/tasks/{task_id}`) отображается:

- **Статус задачи** (`PENDING`, `RUNNING`, `PAUSED`, `COMPLETED`, `FAILED`).
- **Прогресс‑бар** и текстовые сообщения парсера (по этапам работы).
- **Время начала, окончания и длительность** (с учётом пауз).
- Блок **«Настройки поиска»**:
  - компания и сайт;
  - источник (Yandex / 2GIS / оба);
  - **область поиска**:
    - если был выбран один город – `Город: <название>`;
    - если «Страна / общий поиск» – выводится режим и перечень городов, по которым идёт/шла обработка.

#### Кнопки управления задачей

- **⏸ «Приостановить»** – ставит задачу на паузу:
  - прогресс останавливается;
  - время паузы учитывается отдельно.
- **▶ «Возобновить»** (для задач в статусе `PAUSED`) – продолжает парсинг.
- **⏹ «Остановить»** – мягкая остановка:
  - текущий проход (например, текущий город) дорабатывается;
  - дальше задача завершает работу, часть результатов всё равно сохраняется.
- **🔄 «Перезапустить парсинг»** (на паузе) – останавливает текущую задачу и создаёт новую с теми же параметрами.
- **📊 «Показать результаты»** (когда уже есть карточки) – прокручивает страницу к блоку с карточками.

Статус и прогресс обновляются автоматически каждые несколько секунд. После перехода в `COMPLETED` или `FAILED` страница один раз перезагружается, чтобы показать финальные данные.

### 5. Просмотр и выгрузка результатов

1. В блоке **«Агрегированные метрики»** видны сводные показатели по:
   - Яндекс.Картам;
   - 2ГИС;
   - (при необходимости) объединённой статистике.
2. В блоке **«Найденные карточки»**:
   - карточки разбиты по источникам (Яндекс / 2ГИС);
   - внутри каждого источника карточки сгруппированы по **городу**;
   - по каждой карточке можно раскрыть подробности и отзывы.
3. В правом верхнем углу блока метрик, когда задача завершена, появится кнопка:
   - **«📥 Скачать PDF отчёт»** – генерация и скачивание PDF‑версии отчёта.
4. Внизу страницы есть блок **«Файлы и уведомления»**:
   - путь к CSV‑файлу (отчёт для Excel/BI);
   - email, на который отправляется уведомление (если настроен SMTP).

### 6. Дополнительные возможности

- В блоке **«Найденные карточки»** можно дополнительно включить аналитические блоки:
  - галочка **«Показывать проблемные карточки»** показывает:
    - топ‑5 карточек по числу/доле негативных отзывов;
    - топ‑5 карточек без ответов при большом количестве отзывов.
- Эта опция включается/выключается напрямую на странице отчёта, не влияя на сам CSV/PDF.

## Особенности

- ✅ Последовательный парсинг Yandex.Maps и 2GIS с единым отчётом
- ✅ Прогресс-бар и текстовый статус в реальном времени
- ✅ Генерация CSV и PDF‑отчётов
- ✅ Защита веб‑интерфейса паролем
- ✅ Поддержка прокси с аутентификацией
- ✅ Email‑уведомления (опционально)
- ✅ Детальная статистика по карточкам и отзывам:
  - по **Яндекс.Картам**: рейтинг 1–5, оценка тональности (**1–2⭐ негатив / 3⭐ нейтрал / 4–5⭐ позитив**), доля отвеченных отзывов, оценка скорости ответа;
  - по **2ГИС**: карточки организаций, отзывы и ответы компании, общее число отзывов и отвеченных, оценочное количество негативных/позитивных отзывов и примерный общий рейтинг;
- ✅ Опциональные аналитические блоки в веб‑отчёте (включаются по флажку на странице задачи):
  - «Самые проблемные карточки» по негативным отзывам для Яндекса и 2ГИС;
  - «Карточки без ответов» (много отзывов и 0 ответов) по обоим источникам.

Подробнее про ограничения данных и точность метрик см. в `YANDEX_2GIS_DATA_LIMITS.md`.

## Структура проекта

```
unified_parser_v2/
├── config/
│   └── config.json          # Основные настройки
├── src/
│   ├── config/
│   │   └── settings.py      # Загрузка настроек из .env и config.json
│   ├── drivers/
│   │   └── selenium_driver.py
│   ├── parsers/
│   │   ├── yandex_parser.py
│   │   └── gis_parser.py
│   ├── storage/
│   │   ├── csv_writer.py
│   │   └── pdf_writer.py
│   ├── webapp/
│   │   ├── app.py
│   │   ├── templates/
│   │   └── static/
│   └── utils/
│       └── task_manager.py
├── .env                      # Ваши секретные данные (не в git)
├── .env.example              # Пример файла .env
├── run_server.py
└── requirements.txt
```

## Безопасность

⚠️ **Важно**: Файл `.env` содержит секретные данные и не должен попадать в git. Он уже добавлен в `.gitignore`.
